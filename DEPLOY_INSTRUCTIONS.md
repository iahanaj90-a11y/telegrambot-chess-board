# 🚀 Инструкция по деплою обновленного Mini App

## ✅ Что изменилось

- ✨ Добавлено 4 варианта отображения шахматки
- 📊 Показ ФИО владельцев в занятых квартирах
- 🎨 Современный адаптивный дизайн
- 🔍 Поиск и фильтрация квартир
- 📱 Полная адаптивность для всех устройств

## 📝 Шаги для деплоя

### 1. Обновление данных

Перед деплоем обновите JSON файл с актуальными данными:

```bash
cd /Users/hanai/Downloads/telegrambot/bot\ 2/dogovor_bot
python3 export_apartments.py
```

### 2. Проверка локально

Протестируйте приложение локально:

```bash
cd webapp
python3 server.py
```

Откройте в браузере: `http://localhost:8000`

**Важно:** Без Telegram `initData` приложение покажет ошибку доступа — это нормально!

### 3. Загрузка на GitHub

Если у вас уже есть репозиторий на GitHub:

```bash
# Перейдите в папку webapp
cd /Users/hanai/Downloads/telegrambot/bot\ 2/dogovor_bot/webapp

# Если репозиторий не инициализирован
git init
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git

# Добавьте файлы
git add index.html style.css script.js apartments_status.json
git commit -m "✨ Обновлено: 4 варианта шахматки с ФИО владельцев"

# Отправьте на GitHub
git push origin main
```

### 4. Включение GitHub Pages

1. Откройте репозиторий на GitHub
2. Перейдите в **Settings** → **Pages**
3. В разделе **Source** выберите:
   - Branch: `main`
   - Folder: `/ (root)`
4. Нажмите **Save**
5. Подождите 1-2 минуты

### 5. Обновление URL в боте

После деплоя обновите URL Mini App в коде бота (`main_bot.py`):

```python
mini_app_url = "https://YOUR_USERNAME.github.io/YOUR_REPO/"
```

### 6. Проверка

Откройте бота в Telegram и выберите "🏢 Шахматка квартир"

Должны отобразиться:
- ✅ Панель статистики
- ✅ 4 таба для переключения
- ✅ Данные из вашей базы
- ✅ ФИО владельцев в занятых квартирах

## 🔄 Обновление данных в будущем

Когда добавите новых клиентов:

1. Экспортируйте данные:
   ```bash
   cd dogovor_bot
   python3 export_apartments.py
   ```

2. Закоммитьте и отправьте:
   ```bash
   cd webapp
   git add apartments_status.json
   git commit -m "📊 Обновлены данные о квартирах"
   git push
   ```

GitHub Pages обновится автоматически через 1-2 минуты!

## 📱 Альтернатива: Локальный сервер

Если не хотите использовать GitHub Pages, можно использовать:

### Ngrok (простой способ)
```bash
# Запустите локальный сервер
cd webapp
python3 server.py &

# В другом терминале
ngrok http 8000
```

Скопируйте HTTPS URL из ngrok и используйте в боте.

### Serveo (без установки)
```bash
cd webapp
python3 server.py &
ssh -R 80:localhost:8000 serveo.net
```

## ❗ Важные заметки

1. **Безопасность:** Приложение защищено проверкой Telegram `initData`
2. **HTTPS обязателен:** Mini App работает только через HTTPS
3. **Данные актуальны:** Не забывайте экспортировать после добавления клиентов
4. **Кэш браузера:** Если изменения не видны, очистите кэш: Ctrl+Shift+R

## 🎨 Кастомизация

Вы можете настроить:

### Цвета (`style.css`):
```css
:root {
    --color-primary: #3390ec;  /* Основной цвет */
    --color-success: #4caf50;  /* Зеленый (свободно) */
    --color-danger: #f44336;   /* Красный (занято) */
}
```

### Количество этажей и квартир (`script.js`):
```javascript
const floors = ['ц.', '1', '2', '3', '4', '5', '6', '7', '8', '9'];
const apartmentsPerFloor = 14;
```

## 📞 Поддержка

При возникновении проблем:

1. Проверьте логи браузера (F12 → Console)
2. Убедитесь, что JSON файл корректный
3. Проверьте, что HTTPS URL правильный
4. Очистите кэш и перезагрузите

## ✅ Готово!

Теперь у вас современный Mini App с 4 вариантами отображения и полной информацией о квартирах! 🎉

